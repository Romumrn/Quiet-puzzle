# Quiet Puzzle — playable prototype
# Local build output — the published version lives in docs/
prototype/dist/

# Technical document kept confidential, repository is public.
# Canonical file name: Technical_Document_Developers.md
Document_Technique_Developpeurs.md

# Audio: only the MP3 files used by the game are tracked.
# WAV files are intermediate artifacts and can be regenerated with:
# `python3 tools/music.py`
prototype/audio/*.wav
prototype/audio/1-aube.mp3
prototype/audio/2-derive.mp3

.DS_Store
node_modules/
